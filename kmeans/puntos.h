extern float err;
typedef struct DistanciaPunto{
    int p1;
    int p2;
    float dist;
}DistanciaPunto;
unsigned int **iniciar_centroides(Matriz*,int);
//unsigned int **repartir_puntos(DistanciaPunto*,float**,Matriz*,int);
